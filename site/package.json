{
  "name": "marketplace-docs",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Astro Starlight site that documents the plugins and skills in this marketplace, read from the committed Claude Code build at the repo root. Deployed to GitHub Pages.",
  "scripts": {
    "dev": "bun run import && astro dev",
    "build": "bun run import && astro build",
    "clean": "rm -rf dist .astro public/diagrams src/content/docs",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "import": "node ../scripts/import-docs.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/markdown-remark": "^7.2.0",
    "@astrojs/starlight": "^0.39.3",
    "@types/mdast": "^4.0.4",
    "astro": "^6.4.8",
    "typescript": "~6.0.3",
    "vfile": "^6.0.3"
  }
}
